# OIB-Referenzdaten Tirol

Gemeinsame NAT- und TNAT,13-Daten für Klima und Heizlast. Die `.js`-Dateien
werden direkt im Browser geladen; die `.json`-Dateien bleiben als
nachvollziehbare Datenrepräsentation und für Wartungszwecke erhalten.
